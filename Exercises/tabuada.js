const multiplicador = 7;
let iniciador = 0;

for (let iniciador = 0; iniciador <= 10; iniciador++) {
    const calculo = multiplicador * iniciador
    console.log(`${multiplicador} x ${iniciador} = ${calculo}`)
}